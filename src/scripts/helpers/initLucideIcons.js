import { createIcons, Mail, Phone, Menu, X, Send } from 'lucide';

export function initLucideIcons() {
  createIcons({
    icons: {
      Mail,
      Phone,
      Menu,
      X,
      Send,
    },
  });
}
