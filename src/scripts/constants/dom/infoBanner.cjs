'use strict';

/** Info banner visibility (`infoBanner.js`, `info-banner.liquid`). */
const INFO_BANNER_STATE_CLASS = {
  hide: 'hide-banner',
  show: 'show-banner',
};

module.exports = { INFO_BANNER_STATE_CLASS };
