'use strict';

/** Section element ids on the home page (`*.liquid` / Eleventy). */
const SECTION_IDS = {
  services: 'desc',
  stocking: 'stocking',
  videos: 'videos',
  contacts: 'contacts',
};

module.exports = { SECTION_IDS };
