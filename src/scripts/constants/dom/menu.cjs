'use strict';

/** Mobile nav drawer (`sideBar.js`, `menu.liquid`). */
const MENU_CLASS = {
  root: 'menu',
  open: 'open',
  toggler: 'navbar-toggler',
  openMenuIcon: 'open-menu',
  closeMenuIcon: 'close-menu',
};

module.exports = { MENU_CLASS };
